{% extends 'base.html.twig' %}

{% block stylesheets %}
        <link rel="stylesheet" href="login.style.css">
        <link rel="stylesheet" href="style.css">
{% endblock %}

{% block body %}
<div class='container text-center'>
    <h1 class="font-weight-normal text-center">Register</h1>

    {{ form_start(registrationForm) }}
        {{ form_row(registrationForm.username) }}
        {{ form_row(registrationForm.email) }}
        {{ form_row(registrationForm.plainPassword) }}
        {{ form_row(registrationForm.profile_desc) }}

        <button id="sign" class="btn btn-lg btn-primary" type="submit">
            Register
        </button>
    {{ form_end(registrationForm) }}
</div>
{% endblock %}
{# <?php

namespace App\Controller;

use Symfony\Bundle\FrameworkBundle\Controller\AbstractController;
use Sensio\Bundle\FrameworkExtraBundle\Configuration\Route;
use Sensio\Bundle\FrameworkExtraBundle\Configuration\Method;

class MovieController extends AbstractController
{
    /**
     * @Route("/movie/{id}", defaults={"id" = 0}, name="movie")
     * @Method("GET")
     */
    public function index($id)
    {
        $token  = new \Tmdb\ApiToken('2d0f20ce4b63a3992d225fe3751cb1d1');
        $client = new \Tmdb\Client($token);

        $movie = $client->getMoviesApi()->getMovie($id);

        dump($movie);

        return $this->render('movie/index.html.twig', array('movies' => $movie));
            
    }
} #}
